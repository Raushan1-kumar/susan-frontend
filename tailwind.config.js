module.exports = {
  content: ["./src/**/*.{html,js,jsx,ts,tsx}"], // Adjust based on your setup
  theme: {
    extend: {
      colors: {
        primary: "#0D1B2A", // Midnight Blue (Background)
        secondary: "#1B263B", // Deep Blue (Cards, Sections)
        accent: "#00FF99", // Neon Green (Buttons, Highlights)
        textPrimary: "#E0E1DD", // Off-White (Text)
        textSecondary: "#A8A8A8", // Light Gray (Subtext)
        borderColor: "#23395B", // Muted Blue (Borders)
      },
    },
  },
  plugins: [],
};
